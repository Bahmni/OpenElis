<project name="OpenElis" default="dist" basedir=".">
    <description>
        OpenElis build file
    </description>
    <presetdef name="javac">
        <javac includeantruntime="false" />
    </presetdef>
    <property name="src" location="openelis/src"/>
    <property name="test.src" location="openelis/test"/>
    <property name="target" location="openelis/target"/>
    <property name="test.target" location="openelis/test/target"/>
    <property name="dist" location="openelis/dist"/>
    <property name="web" location="openelis/WebContent"/>
    <property name="liquibase" location="liquibase"/>
    <property name="junit.output.dir" value="junit"/>

    <target name="init">
        <tstamp/>
        <mkdir dir="${target}"/>
    </target>

    <path id="lib">
        <fileset dir="${web}/WEB-INF/lib">
            <include name="*.jar"/>
        </fileset>
    </path>

    <target name="compile" depends="init"
            description="compile the source ">
        <mkdir dir="${target}"/>
        <javac srcdir="${src}" destdir="${target}" encoding="iso-8859-1" classpathref="lib" source="1.7" target="1.7" debug="on"/>
        <copy todir="${target}">
            <fileset dir="${src}" includes="**/*.xml"/>
        </copy>
    </target>

    <target name="dist" depends="clean, compile"
            description="generate the distribution">
        <mkdir dir="${dist}/lib"/>
        <copy todir="${target}">
            <fileset dir="${src}" includes="**/*.properties"/>
        </copy>
        <war destfile="${dist}/openelis.war">
            <classes dir="${target}"/>
            <fileset dir="${web}"/>
            <lib dir="${dist}/lib"/>
        </war>
    </target>

    <target name="build" depends="clean, compile, test, dist"
            description="run test and generate the distribution">
    </target>

    <target name="clean" description="clean up">
        <delete dir="${target}"/>
        <delete dir="${dist}"/>
        <delete dir="${junit.output.dir}"/>
        <delete dir="${test.target}"/>
    </target>

    <target name="setupDB">
        <exec executable="dropdb">
            <env key="PGPASSWORD" value="password"/>
            <arg line="-U postgres clinlims"/>
        </exec>
        <exec executable="psql" failonerror="true">
            <env key="PGPASSWORD" value="password"/>
            <arg line="-U postgres -f scripts/setupDB.sql"/>
        </exec>
        <exec executable="pg_restore">
            <env key="PGPASSWORD" value="password"/>
            <arg line="-U postgres -d clinlims db_backup/haitiClinical.backup"/>
        </exec>
        <exec executable="java" failonerror="true" dir="${liquibase}">
            <arg line="-jar -Dfile.encoding=utf-8 lib/liquibase-1.9.5.jar --defaultsFile=liquibase.properties  --contexts=haiti --url=jdbc:postgresql://localhost:5432/clinlims update"/>
        </exec>
    </target>

    <target name="test" depends="clean, compile">
        <mkdir dir="${junit.output.dir}"/>
        <mkdir dir="${test.target}"/>
        <javac srcdir="${test.src}" classpathref="lib" destdir="${test.target}" encoding="iso-8859-1" debug="on">
            <classpath>
                <dirset dir="${target}"/>
            </classpath>
        </javac>
        <junit printsummary="yes" haltonerror="false" haltonfailure="false"
               showoutput="true" failureproperty="test.failed">
            <classpath>
                <fileset dir="${web}/WEB-INF/lib">
                    <include name="*.jar"/>
                </fileset>
                <fileset dir="${liquibase}/lib">
                    <include name="*.jar"/>
                </fileset>
                <pathelement location="${target}"/>
                <pathelement location="${test.target}"/>
            </classpath>
            <batchtest todir="${junit.output.dir}">
                <formatter type="xml"/>
                <fileset dir="${test.src}">
                    <include name="**/*Test*.java"/>
                    <exclude name="**/All*Tests.java"/>
                </fileset>
            </batchtest>
        </junit>
        <fail message="Test failure detected, check test results." if="test.failed" />
    </target>
</project>